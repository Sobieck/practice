#include <stdbool.h>
#include <math.h>

bool same_as_digit_powers(int number, int to_power){
  int sum_of_raised_numbers = 0;
  int intermediate_number = number;

  //while(sum_of_raised_numbers < number && intermediate_number > 1){
  //  int digit = intermediate_number % 10;
  //  sum_of_raised_numbers += pow()

  //}

  if(number == sum_of_raised_numbers){
    return true;
  }

  return false;
}
