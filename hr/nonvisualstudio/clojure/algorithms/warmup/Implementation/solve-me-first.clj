(ns hr.clojure.algorithms.warmup.implementation.solve-me-first)

(defn solveMeFirst [x y]
    (+ x y))
;
;; IO
;(def a (read-line))
;(def b (read-line))
;
;(println (solveMeFirst (Integer/parseInt a) (Integer/parseInt b)))
;
